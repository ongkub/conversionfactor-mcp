{
  "_comment": "วาง block mcpServers นี้ใน claude_desktop_config.json ของคุณ แล้ว Restart Claude Desktop",
  "mcpServers": {
    "conversionfactor": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://conversion.sparkth.io/mcp/sse",
        "--header",
        "Authorization: Bearer YOUR_MCP_KEY_HERE"
      ]
    }
  }
}
